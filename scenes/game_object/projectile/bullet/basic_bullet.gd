extends BaseBullet
class_name BasicBullet


func _ready():
    # 필요한 Component를 추가
    # components.append(ExplosiveComponent.new())  # 예시로 폭발 컴포넌트를 추가
    # components.append(PoisonComponent.new())    # 예시로 중독 컴포넌트를 추가
    super._ready()